# CD BOX用スキャン画像の保存方法

Virtual CD Collection Studioで実物に近い3D CD BOXを作成するには、CD、ジャケット、背面カード、ブックレットなどを画像として保存し、アルバムへ用途を割り当てます。

## 1. スキャン時の推奨設定

- 解像度は300～600 dpiを推奨します。
- 写真ではなく、できるだけフラットベッドスキャナーを使用します。
- JPEGは高画質設定、細かな文字や透明部分を残したい画像はPNGで保存します。
- 自動補正、影除去、強いシャープ化は控え、実物の色と寸法を維持します。
- 部品を傾けず、スキャナー面の水平・垂直に合わせます。
- クロップ前の元スキャンも別途保存してください。アプリ上の回転や用途設定は非破壊ですが、元画像を消した場合の復元には原本が必要です。

## 2. 用意するとよい画像

| 実物 | アプリの「用途」 | 保存内容 |
|---|---|---|
| 表ジャケット | `Front` | 正面表紙だけを正方形で保存 |
| 表紙と裏表紙の見開き | `Front見開き` | 横見開きまたは縦見開きの全体を保存 |
| 表ジャケットの裏面 | `Front裏面` | ブックレットを開いたときの内側 |
| 背面カード全体 | `Spine付きBack` | 左Spine、Back、右Spineを切らずに1枚で保存 |
| 背面だけ | `Back` | 中央の背面パネルだけを保存 |
| Spineだけ | `Spine`、`左Spine`、`右Spine` | 独立してスキャンした細長い背表紙 |
| トレイ下の印刷物 | `Inlay` | トレイを外して見える全面を保存 |
| CD盤面 | `Disc` | 外周とセンターホールを含め、盤面を中央に置いて保存 |
| 2枚のCDを含む画像 | `Disc（2 Disc）` | 2枚を水平または垂直に並べて保存 |
| ブックレット本文 | `PAGE（冊子ページ）`／`ライナーノーツ` | 読む順番で個別画像として保存 |
| 帯 | `Spine Card（帯）` | Back側、Spine、Front側を広げた状態で保存 |
| チラシ・ポスター | `Flyer`／`Poster` | 折り目を含む全体を保存 |

## 3. 画像の保存場所

### 方法A：音源と一緒に保存する

通常の音楽フォルダーでは、音声ファイルと同じアルバムフォルダーへJPG／JPEG／PNGを保存します。ZIP／ZIP.MP3では、音声と画像を同じアーカイブへ入れます。ライブラリを再読み込みすると画像一覧へ表示されます。

分かりやすいファイル名の例：

```text
01-front.jpg
02-front-inside.jpg
03-back-with-spines.jpg
04-inlay.jpg
05-disc.jpg
06-booklet-page-01.jpg
07-obi.jpg
```

ファイル名から自動判定されない場合でも、アプリで正しい「用途」を手動指定できます。

### 方法B：アプリの管理領域へコピーする

1. アプリで対象アルバムを選択します。
2. 「アルバム画像」の「画像を追加」を押します。
3. 保存済みのJPG／JPEG／PNGを1枚または複数選択します。
4. 追加された画像を前後ボタンで表示します。
5. 「用途」から画像に合う項目を選択します。
6. 必要に応じて「↶ 90°」「90° ↷」で向きを保存します。
7. 帯の場合は「帯のBack／Spine範囲」で折り位置を調整します。
8. 「3Dビュー」で配置を確認します。

「画像を追加」は元画像を変更せず、アプリのデータ保存領域へコピーします。Windowsの既定保存先は次の場所です。

```text
%LOCALAPPDATA%\ZipMp3Player\artwork
```

フォルダー名はアルバムごとの識別子です。直接編集せず、追加・削除・用途変更はアプリから行うことを推奨します。

## 4. バックアップ

アプリのバックアップ機能は、管理領域へ追加した画像、用途設定、画像の回転、帯の折り位置を設定・履歴などと一緒に保存します。音楽ファイル自体と、方法Aで音源フォルダー／ZIP内へ保存した原本画像はバックアップ対象外です。これらは別のドライブにも複製してください。

配布版ZIPには利用者個人の音楽、スキャン画像、設定、履歴は含まれません。

## 5. きれいに表示するための要点

- `Disc`はセンターホールが画像中央付近に来るようにスキャンします。アプリは盤面境界を放射状に検出して余白を除去します。
- `Spine付きBack`は左右のSpineを切り落とさず、カード全幅を維持します。
- `Front見開き`は表と裏の左右関係に合う用途を選びます。逆の場合は「左＝表／右＝裏」を選択します。
- `Inlay`を登録するとトレイ越しの背面印刷を再現できます。トレイ色は3D画像欄の「トレイ」で調整できます。
- 元スキャンの縦横比を変形せず、必要な余白除去はアプリ側の処理または複製画像で行ってください。
