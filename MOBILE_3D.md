# モバイル用3Dデータ（試作）

Android版のソース・ビルド手順は [android-player/README.md](android-player/README.md) を参照してください。0.7.1ではアルバム画像右側の立方体アイコンから3Dを開きます。

アルバムの通常の3Dビューを開き、上部の「モバイル3D出力」から `.vcd3d` を保存します。Windowsで設定済みの画像の切り抜き・向き・用途とトレイ色を持ち出します。音源・元画像・設定は変更しません。

今回のv2出力はAndroid Virtual CD Player 0.7.0以降で対象アルバムのタイトル横「3D」→「取込」から読み込みます。対象アルバムと書き出しデータの名前を確認して保存します。SDカードへのコピーは手動です。音楽ZIPの再構築は不要です。Android側では従来のv1データも読み込めます。

Android内蔵の詳細ケース模型を使用します。表・裏・背・表紙内側・インレイ中央・盤面1枚を長辺1024px以下のPNGとして出力します。帯はWindowsの折り位置を適用して表・背・裏へ分割し、表裏の折り幅も保存します。帯ありの場合は包装付きが初期状態です。AndroidでCD出入・帯着脱・包装着脱ができ、ダブルタップで初期状態へ戻せます。

GLBモデル、WindowsのSTL、音源は含みません。2枚目のCD・3Dブックレット・ラック表示は未対応です。ケースの造形・反射や包装の動きはモバイル向けの近似表現で、物理シミュレーションではありません。

ファイルはZIP内のmanifest.jsonと用途別PNGで構成。format=virtual-cd-case、version=2、model=jewel-case-v2。帯のobiFront/obiSpine/obiBack画像とobi.frontWidthMm/backWidthMm、初期包装状態wrappedを追加。画像にはSHA-256を記録。元Windowsパスは含めません。書き出しは同じ保存先フォルダーの一時ファイル完成後に置き換えます。既存パッケージの上書きは保存ダイアログで確認します。
