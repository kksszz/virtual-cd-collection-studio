# モバイル同期（プレビュー）

Windowsのアルバム一覧を右クリック → **モバイル同期（選択して転送）**。
専用ウィンドウでアルバム／アーティスト検索、複数選択、表示中の全選択ができます。

## 通常利用：Wi-Fi（USBデバッグ不要）

1. WindowsとAndroidを同じ信頼できる家庭内LANにつなぎます。
2. Windowsの同期画面でアルバムを選び「選択したアルバムを同期」。PCに転送用コピーを作成します。
3. Androidの設定からSDカードの保存先フォルダーを選択し、書き込みを許可します。
4. Androidの **PCから同期（Wi-Fi） → QRコードを読み取る** でWindowsの接続QRをスキャンします。初回のみカメラ使用を許可し、表示された接続先を確認して「接続して同期」を押します。URL手動入力も予備として残しています。
5. 音楽・画像・GLBを一括取得します。3Dの個別Importは不要です。

Windowsの同期画面は転送中開いておきます。Androidは画面表示中に15秒間隔で差分を確認します。
同期画面を閉じるとPCの配信は終了します。開き直すと接続コードとポートが変わるのでURLを更新してください。
Windowsファイアウォールが尋ねた場合はプライベートネットワークだけ許可します。
このプレビューはHTTPによる家庭内LAN専用です。URLを第三者へ共有せず、ポートをインターネットに公開しないでください。
IPv4プライベートIP、ランダム192bit接続コード、選択アルバムのファイル限定、読み取り専用配信を使用します。
TLS、常駐／バックグラウンド同期、インターネット越し接続は未対応です。QRコードも接続用の秘密情報なので他人に共有しないでください。

## USBデバッグを使わないオフライン転送

「SDカード／フォルダーに出力」でPCに接続したカードへ直接出力できます。
通常のUSB/MTPでは一旦PCフォルダーへ出力し、`vcd-sync.json` と `.vcd-sync` フォルダーを一緒にコピーします。
Androidでその保存先を音楽フォルダーに指定／再読込すると自動認識します。MTPの直接制御は未実装です。
「ADB転送」は開発用の補助方式です。USBデバッグを許可したUSB端末、または接続済みWi-Fi端末を選べます。

## データと安全性

- Windows v0.84.0／Android 0.9.0から、歌詞とお気に入りも出力します。お気に入りはAndroidの同期設定で初回のみ（標準）／毎回／引き継がないを選べます。「今すぐWindowsの状態に合わせる」はAndroid側の変更を上書きするため確認が入ります。逆方向同期や履歴同期は行いません。
- 高精細3D・歌詞・お気に入りを更新するには、Androidを先に更新したうえでWindowsで対象アルバムの転送を再準備してください。転送済みモデルは自動で置き換わりません。

- 音源は再エンコードしません。DIRの画像・歌詞を含むファイル、ZIP内のデータをそのまま転送します。
- 圧縮ZIPは事前にWindowsで無圧縮ZIP.MP3へ変換してください。
- GLBはWindowsの画像用途・トレイ・帯設定から生成します。標準GLBを別ファイルで保持し、音楽を再ZIPしません。
- `vcd-sync.json` はフォーマット `virtual-cd-sync`、version 1。album ID、相対パス、SHA-256、サイズを記録します。
- `.vcd-sync/<album-id>/<music-revision>/…` に音楽、`.vcd-sync/<album-id>/<sha256>.glb` に3Dを配置します。
- IDはWindowsの正規化された絶対ソースパスのSHA-256。元のWindowsパスは送信しません。同名別アルバムを区別できます。移動・改名は新しいIDになります。
- 変更のないデータは再転送しません。新しい内容は別リビジョンへ転送し、検証完了後に索引を更新します。
- Androidは索引の確定したデータだけを登録し、GLBをサイズ・SHA-256・形式検証後にローカルへ反映します。
- 同期から外したアルバム、以前のリビジョン、既存の手動コピー、元音源は自動削除しません。
- PCには転送用キャッシュが必要です。古いリビジョンの整理と容量表示は今後の課題です。
- 既に手動コピーしたアルバムは、名前・曲数が一致する候補について全音声ファイルのSHA-256を照合します。一意に一致した場合は既存アルバムに3Dを紐付け、既存のお気に入り・履歴のURIを維持します。曖昧な一致、改名、タグ内容の相違などでは安全のため同期用コピーを別アルバムとして扱います。転送用コピー自体は保持します。
- 音楽内容を更新するとAndroidの音源URIも変わります。既存の再生中キューは旧データで継続でき、3Dだけの更新では音源URIは変わりません。

## 検証

`android-player/windows-case-tests --sync-test <test-directory> <fixture.glb>`:
差分スキップ、SHA-256、パス拒否、選択対象だけのHTTP配信を確認。
`--serve` を付けると実機試験用に10分間だけ配信します。

Android instrumentation `mode=sync`, `address=<URL>`:
書き込み許可済み音楽フォルダーの下に専用テストフォルダーを作成し、LAN受信、GLB自動登録、音楽認識、差分スキップ、破損時の既存データ保持を確認します。試験生成物と試験設定は終了時に除去・復元します。
