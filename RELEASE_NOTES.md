# Virtual CD Collection Studio リリースノート

変更履歴を新しいバージョンから順にまとめています。各項目の仕様・更新手順は、そのバージョン当時のものです。

## 目次

- [v0.85.0 / Android 0.10.0](#v0850)
- [v0.84.0 / Android 0.9.0](#v0840)
- [v0.83.0 / Android 0.8.0](#v0830)
- [Android 0.7.1（プレリリース）](#android-071)
- [v0.82.0](#v0820)
- [v0.81.0（未公開・v0.82.0に統合）](#v0810)
- [v0.80.0](#v0800)
- [v0.79.0](#v0790)
- [v0.78.0](#v0780)
- [v0.77.0](#v0770)
- [v0.76.0](#v0760)
- [v0.75.0](#v0750)
- [v0.74.0](#v0740)
- [v0.50.0](#v0500)

<a id="v0850"></a>
## v0.85.0 / Android 0.10.0

### Windows v0.85.0

- 「ジャケットを見る」でケースから取り出した後、ブックレットを開いてビューアーへ移行する演出を追加。開く向きと、帯を少し奥へ退避させる動きを調整しました。
- 半角変換後のタグ保存で、スラッシュを含むアーティスト・ジャンルの検証に失敗する問題を修正。
- ZIP／ZIP.MP3からフォルダーへの変換で、Disc1・Disc2などの階層を維持しつつ1アルバムとして登録。一時展開フォルダーや元ZIPの重複表示を防止します。
- 変換内容と引き継ぎの検証が成功した後、確認済みの元ZIPを削除。検証に失敗した場合は元データを保持します。
- 回転・切り抜き・タグ編集のバックアップを、アプリが初めて検出してから7日後に自動削除する設定を追加（初期値ON）。元データを検証できない場合は保持し、処理はアプリ起動中に行います。ごみ箱には移動しません。

### Android 0.10.0（プレリリース）

- アルバム一覧の長押しから、Android上の音楽と関連画像・歌詞・3Dデータをアルバム単位で削除。対象と容量を確認してから実行し、Windows側の元データは保持します。
- フォルダーの全内容が削除対象です。共用の外部画像は削除せず、音楽ルート・他アルバム・確認後に追加されたファイルの保護、途中失敗時の再試行に対応します。
- 削除済みでもWindowsで転送対象に選んで同期すれば、追加の許可操作なしで再転送。「変更なし」で不足ファイルの転送を省略する問題も修正しました。

### 更新手順

Windowsは.NET同梱のx64 ZIPです。旧版を終了して新しいフォルダーへ展開してください。Androidは10以上向け、従来と同じデバッグ署名の試作APKです。アンインストールせず上書き更新してください。音源・実ジャケット・個人設定・署名鍵は配布しません。

<a id="v0840"></a>
## v0.84.0 / Android 0.9.0

### Windows v0.84.0

- ジャケットの90度回転を元画像へ保存。連続した回転は画像を切り替えるときにまとめて反映します。ZIP／ZIP.MP3の画像編集は無圧縮で再構築し、バックアップを作成します。
- 拡大画像ビューアーに切り抜き・削除を集約。切り抜きは±15度の角度微調整、グリッド、プレビューに対応します。メイン画面の画像操作位置を固定し、回転ボタンを整理しました。
- ブックレット閲覧ボタンとスライドショー（間隔・繰り返し）を追加。
- 設定に生成済み画像サムネイルキャッシュの削除を追加。元画像・音源・お気に入りは削除しません。メモリー上の画像を消すには再起動してください。
- 歌詞の自動スクロールを常時表示の見出しへ移動。曲の右クリックからYouTubeで動画を検索できます。
- 最近追加した順のアルバム並べ替え、CUEアルバムの重複表示改善。
- モバイル出力に歌詞とアルバム／曲のお気に入りを追加。QR接続直後の同期待ちと進捗案内を改善。
- 3DケースのFront・Back・Spine・インレイ・帯の接合位置と紙／プラスチックの厚みを調整。ガードレールを維持し、フロント透明板の範囲、帯の復帰、ジャケット取り出し時の干渉、開封後のテープ残留を修正。
- Android向けGLBにWindows共通の高精細メッシュを使用。ヒンジ・留め具・レール・トレイなどの細部を改善しました。

### Android 0.9.0（プレリリース）

- 設定 →「言語 / Language」で日本語／Englishを切り替え。初期値は日本語、選択は次回起動後も保持します。曲名・アルバム名・歌詞本文は翻訳しません。
- Windowsから歌詞を転送し、曲一覧と再生中の画面から表示。
- Windowsのお気に入りを引き継ぎ。「初回のみ（標準）」「毎回Windowsに合わせる」「引き継がない」と、一度だけ合わせる操作を選べます。Android側の変更をWindowsへ戻す双方向同期ではありません。
- 最近追加した順の並べ替え、高精細GLBと従来モデルの読み込み、Spine・ヒンジ・細部形状の改善。
- QR読み取り直後の接続と同期、転送進捗の固定表示を改善。

### 更新手順・制限

Windowsは.NET同梱のx64 ZIPです。旧版を終了し、新しいフォルダーへ展開してください。Androidは10以上向けで、従来と同じデバッグ署名の試作APKです。上書き更新を行い、失敗しても先にアンインストールしないでください。

先にAndroidを更新してから、Windowsで対象アルバムのモバイル転送を再準備・同期してください。既に転送した3Dは自動では高精細版に置き換わりません。歌詞・お気に入りの変更もWindowsで再準備が必要です。元音源・実ジャケット・個人設定・署名鍵は配布しません。

同期は信頼できる同一LAN専用です。Androidの照明・透明感は近似で、Windowsと完全同一ではありません。Androidの2枚目CD・3Dブックレット、履歴の端末間同期、任意の外部GLB、ZIP内CUE、BIN/ISOは対象外です。Windowsの画像編集は元ファイルを変更するため、重要なアルバムは別途バックアップしてください。

<a id="v0830"></a>
## v0.83.0 / Android 0.8.0

### Windows v0.83.0

- QRコードによる家庭内Wi-Fiモバイル同期。音楽・画像・標準GLBをアルバム単位で転送し、Androidで自動登録します。USBデバッグは不要です。
- 同期一覧を1行の表に変更。アルバム、アーティスト、曲数、元ファイル容量、形式、3D・画像の有無を表示。選択合計容量と曲数も表示します（通信量は再利用・生成3Dにより異なります）。
- QR画面にAndroidからの受信進捗・保存検証済み完了を表示。ステータス領域の高さを固定し、進捗の行数変化でQRが動かないよう修正。
- 単一FLAC＋CUEの曲分割表示と再生。CUE付きFLACは専用デコーダーに変更し、どの曲を選んでもアルバム冒頭になる問題を修正。実ファイル11曲の開始PCM、巻き戻し、ギャップレス経路を検証。
- 通常FLACの先読み再生で生じたCOMスレッド間エラーと通常再生への切り替えを修正。
- 標準GLB出力、3Dタッチ開閉・CD取り出し、ヘッダーボタンの省スペース化。
- 音量ノーマライズ、ライブラリ容量表示、データ保存先設定、歌詞欄幅、無圧縮ZIP変換処理の改善も含みます。

### Android 0.8.0（プレリリース）

- QR同期、転送量・割合・再利用量・完了表示、Windowsへの受信状態通知。
- タグの永続キャッシュと復帰時の読み込み改善、ライブラリ復元と同期の競合修正。
- 既定3時間の可変自動停止タイマー、終了音・通知・終了理由記録。
- アルバム／アーティスト順、曲数・形式・3D表示、再生中アーティスト表示と横画面最適化。
- Image/Imagesフォルダーのジャケット表示、Windows Front指定のカバー優先表示。
- 2本指移動、側面でのケース開閉、中心＋外周の2本指でCD取り出し。ジャケット操作をアイコン化。
- Androidの手動3Dファイル取り込みを廃止し、PC同期へ統一。登録済み旧データは維持します。
- 通常フォルダーの単一FLAC＋CUEを解析し曲ごとの再生範囲を指定。Androidでの実音声の曲頭一致は未検証です。

### 配布と制限

Windows x64版は.NETを同梱したZIPです。旧版を終了して新しいフォルダーへ展開してください。Android 10以上向けAPKは従来と同じデバッグ署名の試作版です。上書き更新し、更新できない場合も先にアンインストールしないでください。

Wi-Fi同期は信頼できる同一LAN専用で、公開インターネット用ではありません。更新で別リビジョンとなった同期データは旧コピーが残る場合があります。お気に入り・履歴の端末間同期は未対応です。任意の外部GLB、ZIP内CUE、AndroidでのBIN/ISOは対象外です。タッチ操作感は端末で調整・確認を続けています。

音源・実ジャケット・端末設定・署名鍵は配布に含めません。ビルド、静的チェック、ZIP・タイマー・同期処理・ジェスチャー判定・固定QRレイアウトを検証しています。

<a id="android-071"></a>
## Android 0.7.1（プレリリース）

Virtual CD Playerの初回Android配布版です。Windows版とは別の `android-v0.7.1` タグで公開します。Android 10以上に対応し、Xperia 1 IIで検証しています。

- 通常フォルダーのMP3／FLAC／WAV／M4A、無圧縮ZIP／ZIP.MP3の直接再生。
- ジャケット付きアルバム一覧、タグの曲名・番号、プロパティ、お気に入り、履歴、バックグラウンド再生。
- ギャップレス、音の補正・EQ、速度・ピッチ調整、ジャケットの全画面閲覧。
- Windowsの `.vcd3d` を取り込み、ケース・CD・帯・包装を3Dで鑑賞。タッチ／マウス操作と表示中の再生操作に対応。
- 仮一覧・読み込み進捗の先行表示、タグ情報の再利用、不要な読み込みの中止、画像読み込みの分離。

配布APKは試作・デバッグ署名版です。同じ署名の試作版へ上書き更新できます。更新失敗時にアンインストールするとアプリ内設定等が消えるため、先に原因を確認してください。音源・実アルバム画像・署名鍵は同梱しません。初回のSDカード読み取り時間は残り、タグキャッシュはプロセス内のみです。

圧縮音声エントリー、暗号化ZIP、ZIP64、分割ZIP、CUE／ISOは未対応です。Windowsとのお気に入り・履歴の自動同期、Android上のタグ編集も未対応です。Windows安定版v0.82.0の配布物・最新版指定は変更しません。

詳細は [Android版README](android-player/README.md) と [プレリリースページ](https://github.com/kksszz/virtual-cd-collection-studio/releases/tag/android-v0.7.1) を参照してください。

## v0.82.0

### 今回の変更

- DIR → 無圧縮ZIP.MP3、ZIP.MP3 → DIRの双方向変換を追加しました。音声を再エンコードせず、全ファイルの内容を照合します。同名の出力先には上書きしません。
- ZIP.MP3からの展開フォルダー名は現在のZIP名を使用します。全収録物を包む共通の親フォルダーがある場合のみ一段取り除き、サブフォルダーは保持します。
- 変換時に画像設定・追加画像・歌詞・お気に入り・再生履歴を引き継ぎます。元データの削除は検証後に別途確認し、既定は「いいえ」です。完全削除なのでごみ箱には入りません。詳細はFOLDER_ZIP_CONVERSION.mdをご覧ください。
- タグ保存、画像のZIP再構築、形式変換等の処理中に終了を要求した場合、保存・検証・後処理が完了するまで終了を保留します。タスクマネージャーの強制終了や電源断は防げません。
- Wikipediaのアーティスト・アルバム検索に、日本語版と英語版の選択肢を追加しました。
- 3Dビューを閉じても「表示しています」が残るステータス表示を修正しました。

### v0.80.0からのその他の変更

未公開だったv0.81.0の変更も含みます。CUE付きCD音声ISO/BINの読み込み、外部曲情報の候補選択、アルバム検出の改善、キーボードによるアルバム移動、Disc順の曲並び、3Dビューの無操作時回転、画像検索の補足除去再検索、画像・歌詞の独立した操作メニュー等です。詳細は[v0.81.0の項目](#v0810)および[CUE_SUPPORT.md](CUE_SUPPORT.md)を参照してください。

### 制限と更新方法

変換対象はMP3音源のアルバムです。音源が複数階層に分散したZIP等は対象外です。検証失敗時は元データを保持します。削除途中の通信・権限エラーでは、元フォルダーの一部が残る場合がありますが、検証済みZIPは保持します。

旧版を終了してからWindows x64版ZIPを新しいフォルダーへ展開し、Virtual CD Collection Studio.exeを起動してください。.NET実行環境を同梱しています。既存の設定・画像・履歴は従来の保存場所を引き継ぎます。配布物には個人の音源・画像・設定・履歴を含みません。

---

## v0.81.0

※単独では未公開です。変更内容はv0.82.0に含まれています。

### 主な変更

- CUE付きCD音声ISO/BINの曲別表示・再生・シークに対応しました。MusicBrainzの候補を確認して曲情報を取り込めます。元の音源/CUEは変更しません。単独ISO・SACD・DVD-Audio等は対象外です。詳しくはCUE_SUPPORT.mdを参照してください。
- 追加・有効化したフォルダーを直接確認し、表示対象フォルダーを優先して新しいアルバムを検出します。
- アルバム一覧でキーボード入力による先頭文字・連続文字検索に対応しました。
- 複数Discの曲をDisc番号、曲番号の順で並べます。
- Front画像がある場合のアルバムアイコン選択を改善しました。
- フルスクリーン3Dビューに、30秒の無操作後に滑らかに回転する機能を追加しました。
- シークバーを中央・約半幅にし、ドラッグ中の移動先時刻を表示します。
- タイトルバーにアーティスト名と曲名、再生情報欄にアルバム名を表示します。
- 画像検索で元の名前に候補がない場合、Disc番号やBonus Track等の補足だけを除いて再検索します。元のタグと正式タイトルの括弧表記は保持します。
- アルバム検索欄の幅と位置を安定させ、検索欄を広げました。サイドバーのCover Flow選択肢を削除しました。
- 画像・歌詞それぞれに独立した「操作」ボタンを配置し、3Dビューは常時表示します。一時停止ボタンの文字欠けと3D操作の重なりを修正しました。

### 更新方法

旧版を終了し、Windows x64版ZIPを新しいフォルダーに展開して、Virtual CD Collection Studio.exeを起動してください。.NET実行環境は同梱しています。

既存のライブラリ・画像・設定・履歴は従来の保存場所を引き継ぎます。配布物に個人データは含みません。

### 検証と制限

CUEの曲境界・シーク・保存・スキャン、画像検索、曲順等の自動テストを実施しています。サンプル4枚57曲の読み取りを確認しましたが、すべての音声機器での聴感検証は行っていません。外部曲情報はサービスの登録状況・通信状況により取得できない場合があります。

---

## v0.80.0

### 主な変更

- 音質向上モード・EQ・低音補正・小音量クリア・音量・再生速度の復元中に、初期値が設定へ上書きされる問題を修正しました。
- 起動時は最後に再生したアルバムと曲を選択・表示します。別アルバムの閲覧だけでは最後の再生対象を変更しません。自動再生は行いません。
- 画像のないアルバムのジャケットをバックグラウンドで取得する機能を追加しました。MusicBrainz / Cover Art ArchiveとTheAudioDBを利用し、作品の一致と画質を確認します。設定で有効化できます（既定OFF）。作品名とアーティスト名を取得元へ送信し、音源やローカルパスは送信しません。元の音源・ZIPは変更しません。詳細・利用条件は同梱のAUTOMATIC_ARTWORK.mdを参照してください。
- アルバムブラウザーのタイルは表示範囲を384pxの画像へ非同期で更新します。スクロール中の読み込みを抑え、キャッシュを制限します。
- アルバム画像と歌詞の既定幅を75:25に変更し、仕切りの右クリックで比率プリセットを選べるようにしました。初回更新時に新しい既定比率を適用し、その後の手動調整は保持します。
- 履歴画面は「すべての履歴」を最初に表示し、最終再生日時の新しい順に並べます。
- 上部の「ファイルを開く」ボタンを取り除き、履歴・お気に入り・アルバムブラウザーの幅と間隔を統一しました。Ctrl+Oでのファイル選択は維持しています。
- 画像回転ボタンと歌詞操作ボタンの表示を簡素化しました。

### 更新方法

1. 起動中の旧版を終了します。
2. Windows x64版ZIPを新しいフォルダーへ展開します。
3. `Virtual CD Collection Studio.exe` を起動します。.NET実行環境は同梱しています。

既存のライブラリ、追加画像、設定、再生履歴は従来の保存場所から引き継がれます。
旧版ですでに初期値へ戻った音質設定は、希望の値に設定し直してください。
配布物に個人の音源・画像・設定・履歴は含みません。

---

## v0.79.0

### 主な変更

- アプリ起動時は保存済みライブラリを先にまとめて復元し、アルバムの存在確認と画像準備を低優先度のバックグラウンド処理へ移しました。登録先がネットワークドライブの場合も、起動直後の待ち時間とUI停止を抑えます。
- 起動後の追加アルバム検出は維持しつつ、最初の自動探索を少し遅らせて初期表示との競合を減らしました。
- 3DビューでSpine Card付きケースを開いたときも、キャラメル包装がデフォルトで装着された状態から始まるように戻しました。
- 3D CoverFlowの連続移動中、中央から外れたケースに古いピックアップ用アニメーションが残って前後へ飛ぶことがある問題を修正しました。左右キーの押し続けでも各ケースが順序どおり滑らかに流れます。

### 更新方法

1. 起動中の旧版を終了します。
2. Windows x64版ZIPを任意のフォルダーへ展開します。
3. `Virtual CD Collection Studio.exe` を起動します。

既存のライブラリ、追加画像、設定、再生履歴は従来の保存場所から引き継がれます。

---

## v0.78.0

### 主な変更

- 登録フォルダーへ追加されたアルバムフォルダーや通常ZIPを、全件再スキャンを待たずにバックグラウンドで検出するようにしました。
- 再生履歴画面へ「ライブラリ変更」を追加し、アルバムの追加・削除日時、曲数、保存場所を記録するようにしました。この履歴もアプリデータのバックアップ対象です。
- アルバム画像一覧から不要画像を選び、確認後に削除できるようにしました。ZIP／ZIP.MP3内の画像は一時ファイルで検証しながらアーカイブを再構築し、対象画像だけを除去します。
- ZIP再構築後に古いキャッシュ位置を参照して画像が開けない場合、最新のZIP中央ディレクトリから画像を再検索して復旧するようにしました。
- アルバム画像の初期表示を、Front見開き、Front、Front縦見開きの優先順で選択するようにしました。壊れた画像が混在する場合も、表示可能な次の画像へフォールバックします。
- Spine Cardの印刷面を不透明化し、未登録の裏面を白い紙として表示しました。ケース越しにトレーのモールドや斜めの透明境界が透ける問題を修正しています。
- Spine Cardの取り外し動作とキャラメル包装を復元し、帯付きケースでも包装を任意に再表示できるようにしました。
- 3D CoverFlowの最大25ケース、画像品質、反射表現を維持したまま、各ケースの多数の個別アニメーションを一つの描画同期処理へ統合しました。
- 3D CoverFlowで左右キーを押し続けると一定速度で連続してケースが流れ、キーを離すと最終位置へ滑らかに収束するようにしました。

### 更新方法

1. 起動中の旧版を終了します。
2. Windows x64版ZIPを任意のフォルダーへ展開します。
3. `Virtual CD Collection Studio.exe` を起動します。

既存のライブラリ、追加画像、設定、再生履歴は従来の保存場所から引き継がれます。

---

## v0.77.0

### 主な変更

- 登録フォルダーをバックグラウンド監視し、音源、ZIP／ZIP.MP3、関連画像、歌詞の追加・削除・名称変更をアルバム単位で自動的に差分更新するようにしました。
- ZIP.MP3を含む一部のMP3で、誤ったUTF-16 BOMを持つタグの読み取り、Xingヘッダーによる再生時間取得、標準MP3デコーダーで開けない場合のMedia Foundation再生フォールバックに対応しました。
- タグ編集の半角化対象をファイル名と全角記号へ拡張しました。半角カナ、囲み文字、ローマ数字は保持し、Windowsのファイル名で使用できない記号は安全のため変換しません。
- Front、Back、Spine付きBack、Inlayの白いスキャン外周を保守的に検出し、元画像を変更せず3D表示用に切り出すようにしました。
- Spine Cardの折り位置検出を改善し、スキャンに含まれる白い区切り帯が余分な紙として表示される問題を抑えました。
- ジュエルケースのフロントパネル、上下レール、ジャケット保持爪、左右ストッパーの寸法と配置を実物スキャンに合わせて調整しました。
- トレー背側の幅、縦リブ、溝、段差を再現し、Disc側には不要なモールドを表示しないようにしました。
- Spine紙面と透明ケース／トレーの前後関係を修正し、斜めの透明境界や背後の形状が紙面へ透ける問題を解消しました。
- 3D CoverFlowとCDラックにもトレー背側のモールドを追加し、通常の3D Viewとの外観差を減らしました。

### 更新方法

1. 起動中の旧版を終了します。
2. Windows x64版ZIPを任意のフォルダーへ展開します。
3. `Virtual CD Collection Studio.exe` を起動します。

既存のライブラリ、追加画像、設定、再生履歴は従来の保存場所から引き継がれます。

---

## v0.76.0

### 主な変更

- 音楽ファイルの大量読み込みをバックグラウンド化し、進捗表示中も画面を操作しやすくしました。
- タグ編集でファイル名の選択・コピー・貼り付け・変更に対応しました。拡張子は保護し、音声は再エンコードしません。
- 日本語を含む一部のID3タグ、壊れた画像フレーム、読み取り専用ファイル、ZIP.MP3内のタグ更新を安定化しました。
- タグ編集の一時ファイルがアルバムへ混入する問題を修正し、既存キャッシュからも除外します。
- 選択範囲の全大文字タグを、`SOUL DOCTOR`から`Soul Doctor`のような表記へ変換できるようにしました。
- タグの欠落、曲番号の未設定・重複・欠番・孤立値、ファイル名との不整合、アルバム名表記の混在を読み取り専用で確認できる異常チェックを追加しました。
- 途中から始まる連番や、ファイル名とタグ番号が一定差で並ぶ体系的な命名規則を異常チェックの対象外にしました。
- アルバム一覧の右クリックメニューから、アーティストまたはアルバムをWikipediaで検索できるようにしました。
- 音楽ライブラリ設定で非表示にしたフォルダを、アルバムブラウザー、3D CoverFlow、CDラックからも除外するようにしました。
- 画像を別ウィンドウで開いた際、画面内に全体が収まる初期表示へ修正しました。
- アルバムブラウザーのBack・Spine画像表示と、次のアルバムを開いた際のCD回転状態を修正しました。
- 3D操作中の描画負荷を抑え、見た目を大きく落とさずオブジェクト移動を軽量化しました。
- ジュエルケースのレール、暗色・白色トレイの縦モールド、段差の連続性を実物スキャンに合わせて調整しました。透明トレイでは縦モールドを表示しません。
- 包装フィルムの前面光沢、上部側面の折り目、ティアテープ後の取り外し軌道を改善しました。
- 帯の比率と移動時の前後関係を修正し、ケースと重なって欠ける表示を抑えました。

### 更新方法

1. 起動中の旧版を終了します。
2. Windows x64版ZIPを任意のフォルダーへ展開します。
3. `Virtual CD Collection Studio.exe` を起動します。

既存のライブラリ、追加画像、設定、再生履歴は従来の保存場所から引き継がれます。

---

## v0.75.0

### 主な変更

- 実物スキャンを基準に、ジュエルケース外形を142 × 125 × 10 mmへ調整しました。
- 前面カバー、背面シェル、ヒンジ、上下レール、トレイの位置関係を再構築しました。
- トレイのSpine部を1.5 mm厚とし、ケースの上下レールやSpine画像との重なりを調整しました。
- 上下側面の細かな縦モールドを追加し、ケース外周の縁には出ないよう配置しました。
- Spine表示部と周囲フレームの透明度を分離し、印刷面の視認性を改善しました。
- ブックレットを1.5 mm厚で表現し、前面、背面、折り目へ対応画像を連続して表示します。
- ブックレットの未印刷ページ断面をグレーに変更し、保持爪との位置関係を改善しました。
- Back画像、ブックレット、CD盤面の深度競合によるちらつきを軽減しました。
- CDスキャンを中心から放射状に解析し、背景色が不均一な画像や幅広い外周余白も盤面境界でクロップできるようにしました。
- CD回転を描画フレームへ同期し、フレーム遅延による角度誤差の蓄積を防止しました。
- 3D CoverFlowとCDラックの透明レールおよびSpine位置を3D Viewerへ近づけました。
- 配布物へ「CD BOX用スキャン画像の保存方法」を追加しました。

### 更新方法

1. 起動中の旧版を終了します。
2. Windows x64版ZIPを任意のフォルダーへ展開します。
3. `Virtual CD Collection Studio.exe` を起動します。

既存のライブラリ、追加画像、設定、再生履歴は従来の保存場所から引き継がれます。

---

## v0.74.0

### 新しい製品名

`zip.mp3 Player and Manager Plus` は、本リリースから **Virtual CD Collection Studio** になりました。

音楽ファイルの再生だけでなく、CDケース、ジャケット、Spine Card（帯）、ブックレット、ディスク、包装を丸ごとデジタルライブラリ化し、検索・鑑賞・再生できる現在の機能を表す名称です。

### ライセンス

本ソフトウェアの独自コードは GNU General Public License v3.0 only（GPL-3.0-only）で公開します。配布ZIPにはGPL本文、第三者ライブラリおよび3D素材の著作権表示とライセンス全文を同梱しています。

### 主な内容

- タイル、3D CoverFlow、CDラックを備えたアルバムブラウザー
- 検索、ソート、頭文字フィルター、表示サイズ調整
- Attract Modeによる自動選曲とアルバム表示
- 実物に近いジュエルケース、Spine Card、キャラメル包装の3D表現
- CD、2枚組ディスク、Front見開き、ブックレットなどの画像管理
- 3D Viewerでのディスク操作、ダブルクリック再生・停止、再生中の回転
- 3D Viewer下部の曲名、前の曲、一時停止・再開、次の曲、音量操作
- 既存のZIP.MP3、ZIP、MP3、WAV、FLAC、M4Aライブラリと設定の引き継ぎ

### 更新方法

1. 起動中の旧版を終了します。
2. Windows x64版ZIPを任意のフォルダーへ展開します。
3. `Virtual CD Collection Studio.exe` を起動します。

既存データの保存場所は変更していないため、登録済みライブラリ、画像、履歴、設定はそのまま利用できます。

---

## v0.50.0

当時の製品名：zip.mp3 Player and Manager Plus

Windows 10 / 11 x64向けの公開版です。

### 主な変更

- VBR MP3の再生・時間指定頭出しに対応。フレーム解析による再生時間と平均ビットレートを表示します。
- 旧ライブラリから復元したCBR MP3が再生不可になる不具合を修正しました。
- 再生履歴ダッシュボードを追加し、曲名・アーティスト名などの文字色を改善しました。
- 3D CDケース表示、ケース開閉、CD取り出し、トレイ色・画像パーツ割り当てに対応しました。
- Back・左右Spineに画像がない場合、Frontなどを代用せず無地で表示します。
- カバーフローで中央ボタンドラッグによる移動と、Rキーによる位置リセットを追加しました。

### インストール・更新

1. `ZipMp3Player-v0.50.0-win-x64.zip` をダウンロードして展開してください。
2. 起動中の旧版を終了し、展開先の `ZipMp3Player.exe` を起動してください。

.NET実行環境を同梱しています。初回起動時、旧形式のライブラリ情報を自動更新する場合があります。
個人の設定・再生履歴・音楽・歌詞・ジャケット画像は配布物に含みません。

### 3D操作

- 左ドラッグ：回転
- 中央ボタンドラッグ：移動（通常表示・全画面表示）
- R：移動位置を戻す
- ホイール：通常表示はアルバム切り替え、全画面はズーム
- F11：全画面、Esc：全画面を閉じる
- C：ケース開閉、D：CD取り出し／収納

### 補足

- AIギターリメイク・カラオケ機能は配布版から切り離しています。
- 3D形状に含まれる第三者素材の出典とライセンスは同梱の `THIRD_PARTY_NOTICES.md` を参照してください。
- 音源を用いたCBR/VBRデコード・シーク検証、旧キャッシュ互換性、画面配色、画像パーツ割り当て、中央ボタンドラッグの自動テストを実施しています。
